[#ftl]
[@b.head/]
[@b.toolbar title="校外考试期号设置"]
    bar.addBack();
[/@]
[@b.form name="configForm" action=b.rest.save(config) theme="list"]
    [@b.select  name="config.semester.id" required="true" id="semestersId"  style="width:200px" label="学期"]
        [#list semesters?sort_by("code")?reverse as semester]
        <option value="${semester.id}" [#if (config.semester.id)?? && semester.id==config.semester.id]selected[/#if] title="${semester.schoolYear}学年 ${semester.name?replace('0','')}学期">${semester.schoolYear}学年 ${semester.name?replace('0','')}学期</option>
        [/#list]
    [/@]
    [@b.textfield name="config.name" label="名称" value="${(config.name)!}" required="true" style="width:200px"/]
    [@b.radios  name="config.prediction" required="true"  value=config.prediction  label="是否预报名" /]
    [@b.textfield name="config.maxOptions" label="最多报名科目数" value=config.maxOptions! required="true" /]
    [@b.startend label="报名时间" readOnly="readOnly"  required="true"  name="config.beginAt,config.endAt" required="true" start=config.beginAt end=config.endAt format="yyyy-MM-dd HH:mm:ss" /]
    [@b.textarea name="config.notice"  required="true" maxlength="1000" label="报考须知" value=(config.notice)! cols="80" rows="4"/]
    [@b.formfoot]
        [@b.reset/]
        [@b.submit value="action.submit" /]
    [/@]
[/@]
[@b.foot/]
